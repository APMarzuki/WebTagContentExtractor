# 🚀 WebTagContentExtractor v3.0

Advanced web scraping tool with Selenium support, pagination detection, and GUI interface.

## ✨ Features

- **Dual Scraping Modes**: Simple (static sites) & Selenium (JavaScript sites)
- **Auto Pagination**: Automatically detects and scrapes multiple pages
- **GUI Interface**: User-friendly tkinter-based interface
- **Preset System**: Save and load common scraping configurations
- **CSV Export**: Export results to CSV format
- **Cross-Platform**: Works on Windows, macOS, and Linux

## 🛠️ Installation

### Prerequisites
- Python 3.8+
- Chrome browser (for Selenium mode)

### Install Dependencies
```bash
pip install -r requirements.txt